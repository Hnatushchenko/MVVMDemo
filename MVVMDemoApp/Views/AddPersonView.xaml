<UserControl
    x:Class="MVVMDemoApp.Views.AddPersonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MVVMDemoApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MinWidth="200">
            <TextBlock Text="First Name" />
            <TextBox Text="{x:Bind ViewModel.FirstName, Mode=TwoWay}"/>
            <TextBlock Text="Last Name" />
            <TextBox Text="{x:Bind ViewModel.LastName, Mode=TwoWay}" Margin="0 0 0 5"/>
            <Grid>
                <Button Content="Add" Command="{x:Bind ViewModel.AddPersonCommand}" HorizontalAlignment="Left" />
                <Button Content="Cancel" Command="{x:Bind ViewModel.NavigateToPeopleViewCommand}" HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
